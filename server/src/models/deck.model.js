// Modelo de dados de deck.
// Neste esqueleto, não há conexão com banco, mas este arquivo indica a estrutura de um deck.

export const deckModel = {
  id: 'string',
  title: 'string',
  cards: 'number',
}
